# Environment variables for Trade Alerts SMS Parser

# Discord webhook URL (optional - can also be set via API)
DISCORD_WEBHOOK_URL=

# Price alert Discord webhook URL (separate from regular alerts)
# RECOMMENDED: Set this as an environment variable in your deployment platform (Railway, Heroku, etc.)
# This ensures it persists across redeploys. Can also be set via API endpoint /config/price-alert-webhook
PRICE_ALERT_WEBHOOK_URL=

# Dev Mode Discord webhook URL (for testing without affecting production users)
# When dev mode is enabled, all alerts will be sent to this webhook instead of production webhooks
# Can also be set via API endpoint /config/dev-mode-webhook or saved to dev_mode_webhook.txt
DEV_MODE_WEBHOOK_URL=

# Discord Bot Configuration (for slash commands)
# Get these from https://discord.com/developers/applications
# NOTE: These should be set in your .env file (not committed to git) or as environment variables
DISCORD_BOT_PUBLIC_KEY=
DISCORD_BOT_TOKEN=
DISCORD_APPLICATION_ID=
# Guild ID (Discord Server ID) - Required for guild-specific commands
# To get: Enable Developer Mode in Discord, right-click your server, click "Copy Server ID"
DISCORD_GUILD_ID=
# Server configuration
HOST=0.0.0.0
PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Security (for production)
# WEBHOOK_SECRET=your-secret-key-here
